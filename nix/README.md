# NixOS Configuration Sub-Directory

This folder contains all the nixos related configuration files.

To replicate a nixos build, copy the `configuration.nix` file to `/etc/nixos` and run `sudo nixos-rebuild switch`.
