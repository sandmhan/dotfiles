---
title: Improve NVF AI prompt workflow with mature plugin evaluation
status: needs-follow-up
type: task
updated: 2026-06-01
---

# NVF-031: Improve NVF AI prompt workflow with mature plugin evaluation

## Summary

Analyze the current NVF AI prompt workflow and improve the in-editor experience by evaluating adoption of a mature plugin such as `avante.nvim` for Codex/OpenAI-compatible subscription-oriented workflows.

## Purpose

Provide a stronger interactive AI editing workflow without replacing the existing guarded Pi bridge that remains useful for orchestration and TUI-style tasks.

## Context

Phase 5 delivered a guarded NVF AI bridge that shells out to existing Claude, Codex, and Pi tooling with confirmation and redaction controls. That bridge is intentionally conservative, but the prompt workflow may be less capable than mature in-editor assistant plugins. This ticket tracks a post-roadmap follow-up to evaluate `avante.nvim` or a comparable mature plugin for OpenAI-compatible/Codex-oriented workflows while keeping Pi as a separate guarded orchestration path rather than forcing it into the same UX.

## Acceptance criteria

- [x] The current NVF AI prompt workflow in `home/modules/nvf/ai.nix` and related docs is reviewed, with concrete UX gaps and guardrail requirements recorded.
- [x] `avante.nvim` is evaluated as the preferred candidate, including NVF/Nix packaging feasibility, OpenAI-compatible provider support, Codex/subscription workflow compatibility, keymap ergonomics, context handling, and maintenance maturity.
- [x] A decision is documented to adopt `avante.nvim`, adopt a justified alternative, or defer plugin adoption with clear reasons and follow-up work.
- [x] If a plugin is adopted, it is integrated behind explicit NVF configuration with no committed credentials and no automatic full-buffer or repository-wide context sharing.
- [x] The existing guarded Pi bridge remains available as a distinct orchestration/TUI path, and any overlap with existing leader-a AI keymaps is documented or migrated without removing the guarded Pi workflow.
- [x] Documentation explains provider setup, expected subscription/API-token boundaries, privacy implications, and when to use the plugin workflow versus the Pi bridge.
- [x] Validation evidence covers NVF evaluation/build checks for affected Home Manager profiles plus a runtime Neovim smoke check or a documented reason if plugin runtime validation is deferred.

## Dependencies

- Depends on: [`NVF-021`](NVF-021.md)
- Depends on: [`NVF-022`](NVF-022.md)
- Depends on: [`NVF-023`](NVF-023.md)
- Depends on: [`NVF-024`](NVF-024.md)
- Depends on: [`NVF-027`](NVF-027.md)

## Workflow log

- 2026-06-01: Created with status `open` to track post-roadmap analysis and improvement of the NVF AI prompt workflow with preference toward a mature in-editor plugin while preserving the guarded Pi bridge.
- 2026-06-01: Evaluated Avante.nvim and CodeCompanion.nvim. Adopted CodeCompanion.nvim because it is NVF-supported and better fits production-safe chat, selected-code edit/review/test prompts, and OpenAI-compatible endpoint workflows without weakening the guarded bridge. Added `home/modules/nvf/ai-companion.nix`, `myHome.features.enableNvfAiCompanion`, docs, Phase 7 validation script, and evidence.
- 2026-06-01: Marked done after implementation and validation evidence were recorded in [NVF Phase 7 AI Companion Evidence](../test/evidence/nvf-phase7-ai-companion-2026-06-01.md).
- 2026-06-01: Revised after runtime review found the adapter override under the deprecated top-level path and default slash commands/tools still effectively enabled. Moved the override to the active CodeCompanion HTTP adapter path, added `OPENAI_BASE_URL` fallback handling, configured built-in slash commands/tools as disabled, and expanded Phase 7 validation to inspect resolved runtime CodeCompanion config from the built NVF package.
- 2026-06-01: Revised after follow-up review found CodeCompanion default rules chat autoload still active and an unsupported prompt-library visibility option. Disabled rules chat autoload/default rule callback injection, switched to the installed `display.action_palette.opts.show_prompt_library_builtins` option, hid preset rules, and expanded Phase 7 runtime validation and documentation to reflect the plugin boundary accurately.
- 2026-06-01: Revised after final action-palette and editor-context review found curated prompts hidden while built-in presets and `#` context providers remained exposed. Hid preset actions/prompts/rules, restored curated prompt-library visibility, added a visual action-palette keymap, cleared shared/inline editor-context providers and the `#` trigger through the CodeCompanion hardening extension, and expanded Phase 7 runtime assertions for actual palette contents and disabled editor-context surfaces.
- 2026-06-01: Final review outcome recorded. Nix audit passed, but reviewer status was `needs-revision` because visual CodeCompanion prompt-library entries set `stop_context_insertion = true`, which can suppress selected-code insertion. Reopened as `needs-follow-up` until selected-code prompt payload behavior is fixed and runtime-validated.

## Final outcome

Status: `needs-follow-up`. NVF-031 is not complete because the implementation passed Nix audit but did not pass final review. The remaining blocker is the selected-code CodeCompanion workflow: the curated visual prompts are visible, but `stop_context_insertion = true` can cause review/edit/test/explain actions to submit without the selected text.

## Implementation and revision commits

- `1b4f4ed010e10c1321622b417852416692932a19` — `docs: add NVF AI prompt workflow ticket`
- `27d6ac39f38ec605a61aa9e7d2508af069ad470c` — `feat(nvf): add CodeCompanion AI workflow`
- `c77bc3978c4984b31d3b6677a123ac9e9b65eeed` — `fix(nvf): wire CodeCompanion runtime adapter`
- `5f56abdcc33ec3c675497287656320c0e3e49d7e` — `fix(nvf): disable CodeCompanion rule autoload`
- `bf2458c17cf0288e000cf88cab2d0df40a38bf43` — `fix(nvf): harden CodeCompanion action palette`

## Validation and review

- Worker validation passed: NVF baseline and phase 2-7 scripts, Phase 7 shell syntax and ShellCheck, `nixfmt --check`, `git diff --check`, Home Manager dry-runs for `terminalman`, `sandmhan`, `wslman`, and `macman`, build-local NVF `checkhealth`, and command-existence smoke checks for `:CodeCompanionChat` and `:NvfAiAsk`.
- Nix audit passed with no blocking issues. The only noted warning was the pre-existing Home Manager `gtk.gtk4.theme` legacy-default warning from `home.stateVersion = "24.11"`.
- Final reviewer status: `needs-revision`. Critical issue: selected-code visual prompts currently suppress automatic selection insertion; add a runtime assertion proving selected text reaches at least one chat prompt and the inline edit prompt before closing.

## Limitations

- CodeCompanion does not inherit the guarded bridge's redaction, sensitive-path blocking, confirmation summary, or full-buffer-selection guardrails.
- Runtime checks used a build-local NVF package rather than Home Manager activation.
- Normal-mode `<leader>aA` currently opens a palette with no curated entries because the curated prompts are visual-mode only and built-ins are hidden.

## Follow-ups

- Remove or replace `stop_context_insertion = true` for curated visual selected-code prompts, or explicitly inject the selected range into the prompt payload.
- Extend `scripts/check-nvf-phase7.sh` to verify selected text reaches at least one chat prompt and the inline edit prompt.
- Decide whether normal-mode `<leader>aA` should be removed, documented as intentionally unavailable, or populated with safe normal-mode entries.
- Re-run validation and final review; mark `done` only after both review and audit pass.

## Links

- Requirements: [NVF Enterprise Polyglot IDE Improvement Report](../nvf-enterprise-polyglot-ide-improvement-report.md)
- Plan: Post-roadmap AI prompt workflow improvement
- Evidence: [NVF Phase 7 AI Companion Evidence](../test/evidence/nvf-phase7-ai-companion-2026-06-01.md)
